import React from 'react';
import Typography from "@mui/material/Typography";

const ChartControlsTitle = () => {
    return (
        <Typography variant="h5" component="h2" textAlign="center">
            chart controls
        </Typography>
    );
};

export default ChartControlsTitle;